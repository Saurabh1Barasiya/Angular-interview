import { ChangeapplyDirective } from './changeapply.directive';

describe('ChangeapplyDirective', () => {
  it('should create an instance', () => {
    const directive = new ChangeapplyDirective();
    expect(directive).toBeTruthy();
  });
});
